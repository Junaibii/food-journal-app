diff --git a/node_modules/react-native-reanimated/plugin/index.js b/node_modules/react-native-reanimated/plugin/index.js
index 1234567..abcdefg 100644
--- a/plugin/index.js
+++ b/plugin/index.js
@@ -1,3 +1,7 @@
 // @ts-ignore plugin type isn't exposed
-const plugin = require('react-native-worklets/plugin');
+// Patched: use path.join to resolve react-native-worklets relative to this
+// file rather than relying on bare-specifier Node resolution, which fails
+// inside Metro's jest-worker child process context.
+const path = require('path');
+const plugin = require(path.join(__dirname, '../../react-native-worklets/plugin'));
 
 module.exports = plugin;
